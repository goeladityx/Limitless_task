# the app connects as a role WITHOUT bypassrls, and owns nothing
DATABASE_URL=postgresql://pellet_app:pellet_app@localhost:5432/pellet
# migrations only. this one is the owner and does bypass rls, which is why 003_guard exists
ADMIN_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pellet
